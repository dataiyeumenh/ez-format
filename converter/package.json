{
  "name": "ezformat-converter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "test": "python -m pytest -q"
  }
}
